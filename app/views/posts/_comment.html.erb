<li class="comment">
    <span class="comment-inner">
        <%=comment.author.Name%>: <%=comment.Text%>
    </span>
    <% if can? :manage, @comment %>
<%= button_to "Delete comment", post_comment_path(comment.post_id,comment.id), method: :delete, class: 'delete-btn click' %>
<% end %>
</li>